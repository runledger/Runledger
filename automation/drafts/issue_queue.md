# Issue-first queue (deterministic CI check)

Use this phrasing consistently:

> Deterministic CI regression check for agent/tool calls (record once locally, reuse in CI — no network/secrets)

Proof (merged):
- https://github.com/hoangsonww/Agentic-AI-Pipeline/pull/24
- https://github.com/zhongyu09/openchatbi/pull/8

---

## Target 1: AgentOps-AI/agentops

Proposed issue title:
Optional deterministic CI regression check for agent tools?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 3: crewAIInc/crewAI

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 4: langchain-ai/langgraph

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 5: run-llama/llama_index

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 6: OpenDevin/OpenDevin

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 7: OpenInterpreter/open-interpreter

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 8: griptape-ai/griptape

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 9: OpenBMB/AgentVerse

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 10: Significant-Gravitas/AutoGPT

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?

---

## Target 2: microsoft/autogen

Proposed issue title:
Optional deterministic CI regression check for tool-using agents?

Proposed issue body:
Hi! I'm working on RunLedger, a small deterministic CI check for agent/tool calls. You record tool outputs once locally, then CI reuses them (no network, no secrets). It adds a tiny eval suite + workflow and is optional/removable.

If you're open to it, what's the best existing agent/example entrypoint to wire to for a minimal demo?
